Role 1: 
every model id is int and autoincrement() and have slug means:
 suppose user {id: 1} so user {slug: u-00001} like this

for this, create user => user update slug = `u-{i.padding(5, '0')} like this

---