enum Layout {
  SINGLE
  DOUBLE
  TRIPLE
  QUADRUPLE
  LIST_WITH_IMAGE
  LIST_NO_IMAGE
}

enum OrderType {
  DINE_IN
  TAKEAWAY
}

enum OrderStatus {
  PENDING // order created
  PENDING_PROCESSING // waiting send-to-production
  PROCESSING // sent to production
  READY // all items ready
  PICKED_UP // collected
  CANCELLED // cancelled by customer or staff
}

enum OrderSource {
  QR_TABLE
  TOUCHSCREEN
  STAFF
  ADMIN
}

enum PaymentMethod {
  CASH
  TRANSFER
  OTHER
}

enum PaymentStatus {
  PENDING
  PAID
  CANCELLED
}

enum ItemLabel {
  NEW_MENU
  BEST_SELLER
  CHEF_RECOMMENDATION
  MENU_FAVORITE
  SPICY
  VEGETARIAN
  SIGNATURE_MENU
  KIDS_MENU
  FAST_SERVE
}

enum ItemType {
  INDIVIDUAL
  PACKET
}

enum UserRole {
  OWNER
  ADMIN
  SERVICE
  USER
}

enum Orientation {
  LANDSCAPE
  PORTRAIT
}

enum ShiftType {
  OPENING
  CLOSING
}

enum NotificationEvent {
  ORDER_RECEIVED
  ITEM_READY
  ITEM_PICKED_UP
  MANUAL_REMIND
  ITEM_CANCELLED
}
