model ProductionStation {
  id Int @id @default(autoincrement())

  slug String? // ps-00001

  name      String?
  sortOrder Int?     @default(autoincrement())
  isActive  Boolean? @default(true)

  items      Item[]
  orderItems OrderItem[]

  @@map("production_stations")
}
